<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Keeper</title>
    <link rel="stylesheet" href="/styles/styles.css">
    <script src="https://kit.fontawesome.com/b60c4c06dc.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Agbalumo&display=swap" rel="stylesheet">
</head>
<body>
<h1 style="font-weight: bold;">Book<span class="agbalumo-regular">Keeper</span></h1>


<form action="/" method="post">
    <div class="wrap">
        <div class="search">
           <input type="text" class="searchTerm" name='bookTitle' placeholder="Which book are you looking for?">
           <button type="submit" class="searchButton">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </div>
     </div>
</form>

<div>
    <% if (data) { %>
        <h3>Title: <%= data.title || "Unknown"%> </h1>
        <h3>Author: <%= data.author_name ? data.author_name[0] : "Unknown" %></h3>
        <h4>Published Year: <%= data.first_publish_year || "Unknown" %></h4>

        <% if (bookCover) { %>
            <img src="<%= bookCover %>" alt="Book Cover">
        <% } else { %>
            <p>No cover available.</p>
        <% } %>

    <% } else if (error) { %>
        <h3 style="color: red;"><%= error %></h3>
    <% } %>
</div>

</body>
</html>